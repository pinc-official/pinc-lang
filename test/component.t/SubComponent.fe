component SubComponent {
  let content = #String;
  let num = #Int;
  let array = #Array(of: #Int);

  #Template {
    <div class="SubComponent" data-foo={num}>
      {content}
      {array}
    </div>
  }
}