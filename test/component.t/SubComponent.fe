component SubComponent {
  let content = #String();
  let num = #Int(); // This is currently intentionally wrong!

  #Template {
    <div class="SubComponent" data-foo={num}>
      {content}
    </div>
  }
}