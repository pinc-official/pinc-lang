component Reference(
  label: "Reference",
  icon: "/images/icons/duplicate.svg",
  group: "Structure"
) {
  ref = @ContentSelect(label: "Content Item", instanceOf: [!Section]);

  @Template {
    {ref.@render}
  }
}
