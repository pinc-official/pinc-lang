component Reference(
  label: "Reference",
  icon: "/images/icons/duplicate.svg",
  group: "Structure"
) {
  ref = 
    @Ui(label: "Content Item")
    @ContentSelect(instanceOf: [!Section]);

  @Template {
    {ref.@render}
  }
}
